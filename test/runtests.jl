using BioMakie
using Test

@testset "BioMakie.jl" begin
    # sv = viewstruc("2vb1")
    # @test typeof(sv.residues) == Node
    # @test typeof(sv.residues[]) != Node
    #
    # mv = viewmsa("pf00062")
    # @test typeof(mv.matrix) == Node
    # @test typeof(mv.matrix[]) != Node
end
