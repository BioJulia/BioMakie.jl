# # # BioMakie.jl Documentation

# ```@meta
# CurrentModule = BioMakie
# ```

# You can import the latest code from the master branch.  

using Pkg
Pkg.add("https://github.com/kool7d/BioMakie.jl#master")

using BioMakie

#md # BioMakie uses GLMakie as a backend by default, so to use a different one, just `activate!` it like 
#md # `WGLMakie.activate!()`.
